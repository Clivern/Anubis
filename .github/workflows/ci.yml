name: ci

on:
  push:
    branches:
      - v2

jobs:
  ci:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run CI
        run: |
          pip --version
          pip install opswork
