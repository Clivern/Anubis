name: CI

on:
  push:
    branches:
      - v2

jobs:
  CI:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run CI
        run: |
          pip --version
          pip install opswork
